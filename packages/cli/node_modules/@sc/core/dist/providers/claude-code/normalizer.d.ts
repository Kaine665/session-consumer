/** Decode a Claude Code project slug back to a filesystem path. */
export declare function decodeSlug(slug: string): string;
/** Encode a filesystem path into a Claude Code project slug. */
export declare function encodeSlug(projectPath: string): string;
/** Convert a slug to the directory name on disk. */
export declare function slugToDirname(projectPath: string, slug: string): string;
//# sourceMappingURL=normalizer.d.ts.map